/**
 * Provides hash computing utilities and digest implementations.
 * <p>
 * This package integrates different parsing and checksum algorithms (such as
 * CRC32, MD5, and SHA-1) into a unified abstract interface represented by
 * {@link jrm.digest.MDigest}.
 * </p>
 * 
 * @author optyfr
 */
package jrm.digest;
