/**
 * Provides concrete container and entry repair action tasks used during the fix process.
 * <p>
 * This includes adding entries to archives, deleting directories, duplicating files inside containers, renaming files, backing up
 * original game packages, and preparing torrent-zipped archives.
 * </p>
 *
 * @since 1.0
 * 
 * @author optyfr
 */
package jrm.profile.fix.actions;
