/**
 * Provides specialized classes and interfaces to read, parse, and validate headers
 * of Compressed Hunks of Data (CHD) files (MAME's standard format for compressed disk images).
 * Supports CHD versions 1 through 5, extracting cryptographic checksums (SHA-1 and MD5)
 * and identifying format metadata.
 *
 * @author optyfr
 */
package jrm.io.chd;
