/**
 * Provides classes and interfaces for parsing, representing, and verifying
 * torrent files (.torrent) and their metadata within the Rom Manager. Supports
 * bencoded metadata format and multi-file torrent layouts.
 *
 * @author optyfr
 */
package jrm.io.torrent;
